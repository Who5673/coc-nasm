# About `nasm-to-wasm` 

Not all the commands in NASM is supported by me, but you can use some basic ones like `mov`, `add`, `sub`, `mul`, `div` and `idiv`.  
Here are the instructions to use `./nasm-to-wasm`

## Compile to WebAssembly Text:
```
./nasm-to-wasm -i 
```
